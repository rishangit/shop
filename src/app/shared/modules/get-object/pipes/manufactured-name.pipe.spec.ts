import { ManufacturedNamePipe } from './manufactured-name.pipe';

describe('ManufacturedNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ManufacturedNamePipe();
    expect(pipe).toBeTruthy();
  });
});
