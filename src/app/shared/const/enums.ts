export enum UserType {
    OWNER = 0,
    ADMIN = 1,
    USER = 2
}

export enum QuantityUnits {
    MILLIGRAM = 0,
    GRAM = 1,
    KILOGRAM = 2,
    POUND = 3,
}

export enum dbType {
    SHOP_DB = 1,
    STOCK_DB = 2
}
