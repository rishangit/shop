import { CatagoryNamePipe } from './catagory-name.pipe';

describe('CatagoryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CatagoryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
